// Transition component kept as a no-op — page transitions are now
// handled by AnimatePresence in MainLayout + motion.div in LoaderLayout.
const Transition = () => null;

export default Transition;
